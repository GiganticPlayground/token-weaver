{
  "customInstructions": "Always use yarn for package management commands instead of npm. Use 'yarn add' instead of 'npm install', 'yarn add -D' instead of 'npm install -D', etc. When writing TypeScript imports, never include file extensions (.js, .ts, etc.) in relative import paths."
}
