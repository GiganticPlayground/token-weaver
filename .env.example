# ========================================
# OpenAPI-First API - Environment Variables
# ========================================
# Copy this file to .env and configure as needed:
#   cp .env.example .env

# ========================================
# Server Configuration
# ========================================

# Port number for the Express server
# The API will be accessible at http://localhost:{PORT}
# Docker exposes this port (configured in docker-compose.yml)
# Default: 3000
PORT=3000

# Application Environment
# Controls logging behavior, error messages, and other environment-specific features
# - development: Verbose logging, detailed error messages
# - production: Minimal logging, generic error messages for security
# - test: Used for testing environments
# Valid values: development, production, test
# Default: development
NODE_ENV=development

# ========================================
# API Configuration (Optional)
# ========================================

# Uncomment and configure these if your API requires additional settings

# API Base Path
# Prefix for all API routes (e.g., /api/v1)
# API_BASE_PATH=/api/v1

# CORS Origins
# Comma-separated list of allowed origins for CORS
# Example: http://localhost:3000,https://yourdomain.com
# CORS_ORIGINS=*

# ========================================
# Database Configuration (Optional)
# ========================================

# Add your database connection string if needed
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ========================================
# Security Configuration
# ========================================

# API Keys Configuration File Path
# Path to the API keys JSON file for authentication
# Default: config/api-keys.json
# The default dev key is 'dev-local-key-12345' (for local development only)
# API_KEYS_CONFIG_PATH=config/api-keys.json

# JWT Secret for authentication (if implementing additional auth)
# JWT_SECRET=your-secret-key-here

# API Rate Limiting (Optional)
# Maximum requests per window per IP
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=900000
